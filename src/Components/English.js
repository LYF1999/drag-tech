import React, { PropTypes } from 'react';

class English extends React.Component {

  static propTypes = {};
  static defaultProps = {};

  render() {
    return (
      null
    );
  }
}

export default English;
